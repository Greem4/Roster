/** Право на создание и редактирование лекарств (синхронно с backend). */
export const PERM_RX_MANAGE = 'users:manage'

/** Главная страница RosterRX — см. constants/routes.js (единый источник). */
export { RX_HOME } from '../../constants/routes'
